import css from './Header.css'

import React from 'react';

const Header = () => {
    return (
        <div className={'header'}>
            <h1>Thats movies</h1>
        </div>
    );
};

export default Header;
